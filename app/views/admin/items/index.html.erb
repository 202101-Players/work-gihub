<div class="container px-5 px-sm-0">
  <div class="row">

    adomin itemu index page
    商品一覧

    <table class='table table-hover table-inverse'>
    <thead>
      <tr>
        <th></th>
        <th>商品ID</th>
        <th>商品名</th>
        <th>税抜価格</th>
        <th>ジャンル</th>
        <th>ステータス</th>
        <th colspan="6"></th>
      </tr>
    </thead>
    <tbody>
       <% @items.each do |item| %>
        <tr>
          <td><%= item.id %></td>
          <td><%= item.name %></td>
          <td><%= item.price %></td>
          <td><%= item.genre_id %></td>
          <td><%= item.s_active %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  </div>
</div>