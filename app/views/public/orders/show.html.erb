<h3>注文履歴詳細</3>

<h3>注文情報</h3>


<h5>注文日</h5>
<%= @order.created_at.strftime('%Y/%m/%d') %>

<h5>配送先</h5>
<〒<%= @order.postal_code %><br><%= @order.address %><br><%= @order.name %>

<h5>支払方法</h5>
<%= @order.payment_method %>

<h5>ステータス</h5>
<%= @order.status_i18n %>

<h3>請求情報</h3>

<h5>商品合計</h5>
<%= @order.total_payment %>円

<h5>配送料</h5>
<%= @order.ship_cost %>円

<h5>ご請求額</h5>
<%= ((@order.total_payment)+(@order.ship_cost)) %>円

<h3>請求情報</h3>

<h5>注文内容</h5>
<table>
  <thead>
    <tr>
      <td>商品</td>
      <td>単価（税込）</td>
      <td>個数</td>
      <td>小計</td>
    </tr>
  </thead>
  <% @order_items.each do |order_item|%>
  <tr>
    <td><%= order_item.item.name %></td>
    <td><%= order_item.price %></td>
    <td><%= order_item.amount %></td>
    <td><%= order_item.price*order_item.amount %></td>
  </tr>
<%end%>

</table>